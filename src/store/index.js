import { createStore } from 'vuex'
import tab from './tab'
import getters from './getters'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    tab
  },
  getters: getters
})
